# fs-db-engine-sqlite — SQLite engine translations (English)

db-sqlite-capability-name = SQLite Database Engine

db-sqlite-error-connect = Failed to connect to SQLite database: { $url }
db-sqlite-error-health = SQLite health check failed: { $reason }
db-sqlite-error-query = SQLite query failed: { $reason }
db-sqlite-error-execute = SQLite execute failed: { $reason }
db-sqlite-error-migrate = SQLite migration failed: { $reason }
db-sqlite-error-not-connected = SQLite engine is not connected.

db-sqlite-info-connected = Connected to SQLite at { $path }.
db-sqlite-info-ready = SQLite engine ready (version { $version }).
